# การนำขึ้น Web Hosting (DirectAdmin)

คู่มือสำหรับการนำระบบไปติดตั้งบน Hosting ที่ใช้ DirectAdmin (PHP + MariaDB)

## สิ่งที่ต้องเตรียม
1. ไฟล์ **Source Code** (โฟลเดอร์ `src/`)
2. ไฟล์ **Database SQL** (`database.sql`)

## ขั้นตอนที่ 1: เตรียมไฟล์
1. ตรวจสอบว่ามีไฟล์ `database.sql`
   - เนื่องจากใน Container อาจไม่มีคำสั่ง `mysqldump` ให้ใช้คำสั่ง PHP ที่เตรียมไว้แทน:
     ```bash
     docker exec shift_scheduler php /var/www/html/db_export.php > database.sql
     ```
2. บีบอัดโฟลเดอร์ `src` เป็นไฟล์ ZIP (เช่น `web-app.zip`) โดยข้างในควรมีไฟล์ `index.php`, `config/`, `views/` ฯลฯ

## ขั้นตอนที่ 2: สร้างฐานข้อมูล (DirectAdmin)
1. Login เข้า **DirectAdmin**
2. ไปที่เมนู **MySQL Management** -> **Create New Database**
3. ตั้งชื่อ Database และ User (เช่น `username_scheduler`)
4. ตั้งรหัสผ่าน (Password) -> **จดไว้ให้ดี!**
5. กด Create

## ขั้นตอนที่ 3: Import Database (phpMyAdmin)
1. ใน DirectAdmin ไปที่ **phpMyAdmin**
2. Login ด้วย User/Password ที่เพิ่งสร้าง
3. เลือก Database ทางซ้ายมือ
4. ไปที่ tab **Import**
5. เลือกไฟล์ `database.sql` จากเครื่องคอมพิวเตอร์
6. กดปุ่ม **Go** เพื่อนำเข้าข้อมูล
   - รอจนขึ้นข้อความสีเขียวว่าสำเร็จ

## ขั้นตอนที่ 4: อัพโหลดไฟล์
1. ใน DirectAdmin ไปที่ **File Manager**
2. เข้าไปที่โฟลเดอร์ `public_html`
3. **กรณีติดตั้งใน Subdirectory (เช่น /reg):**
   - สร้างโฟลเดอร์ `reg` ใน `public_html`
   - เข้าไปในโฟลเดอร์ `reg`
4. อัพโหลดไฟล์ `web-app.zip` ที่เตรียมไว้
5. คลิกขวาที่ไฟล์ -> **Extract**
6. ย้ายไฟล์ทั้งหมดออกมาไว้ที่ Root ของโฟลเดอร์ที่ต้องการ (เช่น `public_html/reg/`)
   - โครงสร้าง:
     ```
     public_html/
     └── reg/
         ├── index.php
         ├── config/
         ├── views/
         ...
     ```
   *หมายเหตุ: ระบบได้รับการแก้ไขให้ใช้ Relative Paths ทำให้สามารถติดตั้งในโฟลเดอร์ใดก็ได้โดยไม่ต้องแก้ Code เพิ่มเติม*

## ขั้นตอนที่ 5: ตั้งค่าการเชื่อมต่อฐานข้อมูล
1. ใน File Manager ให้แก้ไขไฟล์ `config/database.php`
2. คลิกขวา -> **Edit**
3. แก้ไขส่วน `getenv(...)` ให้เป็นค่าจริงของ Hosting:

   ```php
   // บรรทัดประมาณ 13-16
   $host = 'localhost'; // ปกติ DirectAdmin ใช้ localhost
   $dbname = 'username_scheduler'; // ชื่อ DB ที่ตั้งในขั้นตอนที่ 2
   $user = 'username_scheduler';   // ชื่อ User ที่ตั้งในขั้นตอนที่ 2
   $pass = 'YOUR_PASSWORD';        // รหัสผ่านที่ตั้งในขั้นตอนที่ 2
   ```

   **ตัวอย่างการแก้ Code:**
   เปลี่ยนจาก:
   ```php
   $host = getenv('DB_HOST') ?: 'db';
   ```
   เป็น:
   ```php
   $host = 'localhost';
   ```
   (หรือจะแก้ตรงค่า Default `'db'` ให้เป็น `'localhost'` ก็ได้)

4. กด **Save**

## ขั้นตอนที่ 6: ทดสอบใช้งาน
1. เปิด Browser เข้าเว็บไซต์ของคุณ (เช่น `yourdomain.com`)
2. หน้าเว็บควรขึ้นให้ใส่รหัสผ่าน (`1234` หรือ `regtu2569` ตามที่ตั้งไว้)
3. ลอง Login เข้า Admin ที่ `yourdomain.com/admin/`

## ข้อควรระวัง
- **Security:** ตรวจสอบไฟล์ `migration.php` หากไม่ได้ใช้แล้วควรลบออกจาก Server
- **PHP Version:** ตรวจสอบใน DirectAdmin ว่า Domain ใช้ PHP Version 8.0 ขึ้นไป (แนะนำ 8.2)
- **Timezone:** หากเวลาไม่ตรง ตรวจสอบการตั้งค่า `date.timezone` ใน PHP Selector หรือแจ้ง Support ยืนยันว่าใช้ `Asia/Bangkok`

---
**ขอให้สนุกกับการใช้งานครับ!** 🚀
